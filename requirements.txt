# Core OpenEnv framework
openenv-core>=0.2.2

# Server-side dependencies for the OpenEnv backend
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0

# Physics Engine dependencies
numpy>=1.26.0

# Inference script client (Mandatory per hackathon rules)
openai>=1.14.0


python-dotenv
